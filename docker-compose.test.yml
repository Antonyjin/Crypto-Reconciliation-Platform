version: '3.8'
services:
  postgres-test:
    image: postgres:16
    environment:
      POSTGRES_DB: crypto_recon_test
      POSTGRES_USER: antonyjin
      POSTGRES_PASSWORD: password
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data
